## Packages
framer-motion | Page transitions, hover effects, and scroll animations
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes intelligently

## Notes
Tailwind Config needs to support the neon color palette and custom fonts.
Using Google Fonts: 'Outfit' for headings (Display) and 'DM Sans' for body.
Backend API endpoints are defined in @shared/routes.
